import { graphql, Link } from 'gatsby';
import React from 'react'
import '../../styles/global.css'


const Projects = () => {
    return ( 
        <div className={"section"} style={{height: "50vw"}} id="projects">
            <h1>Projects</h1>
        </div>
    );
};

export default Projects;